# Core data fetching and simulation scripts run on the Python standard library.
# matplotlib powers the PDF/PNG infographic generator.
# pytest supports the documented quickstart test command.
matplotlib>=3.8
pytest>=8.0
